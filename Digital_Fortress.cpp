#include<bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin>>T;
    while(T--){
        string s;
        getchar();
        getline(cin,s);
        int l=s.size();
        if(pow(l,0.5))
    }
}
