#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        string s;
        cin>>s;
        int sz=s.size();
        int one=0,two=0,big=0;
        for(int i=0;i<sz;i++)
        {
            if(s[i]=='('){
                one++;
            }
        }
    }
}
